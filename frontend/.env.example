# URL base del backend (el proxy de Vite redirige /api → esta URL)
# En desarrollo, Vite hace proxy automático al puerto 3000
VITE_API_BASE_URL=http://localhost:3000

# Puerto del servidor de desarrollo de Vite (opcional, default 5173)
# VITE_PORT=5173
